#include<stdio.h>
int main(){
    char x[100];
    scanf("%[^\n]c",x);
    for(int i=0 ; i<n-1 ; i++){
        for(int j=i+1 ; j<n ; j++){
            if(x[i]!=x[j]);
            // printf("%c",x[i]);
        }
        printf("%c",x[i]);
         break;
    }
    return 0;
}